<h2>Olá, bem vindo!</h2>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>

<div class="form-inputs">
  <%= f.input :email, required: true, label: "Indique seu email corporativo", autofocus: true %>
  <%= f.error :corporate_email_validator, class: "invalid-feedback"  %>
  <%= f.input :password, required: true, label: "Crie uma Senha", hint: ("#{@minimum_password_length} caracteres no mínimo" if @minimum_password_length) %>
  <!-- <%= f.input :password_confirmation, required: true %> -->
</div>


<div class="form-actions">
  <%= f.button :submit, "Inscrever minha empresa 100% grátis", class: "btn-sessions" %>
</div>
<% end %>

<%= render "users/shared/links" %>
