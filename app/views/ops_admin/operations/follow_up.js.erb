document.querySelector('#operation-cards').insertAdjacentHTML('beforeend', '<%= j render partial: "shared/cards/operation_cards/follow_up/operation_cards", locals: {operations: @operations} %>');
<% if @operations.next_page %>
  document.querySelector('.pagination').innerHTML = '<%= j will_paginate @operations %>';
<% else %>
  document.querySelector('.pagination').remove();
<% end %>
