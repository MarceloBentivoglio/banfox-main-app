<%# TODO: Correct this %>
document.querySelector('.table_wo_bundle').insertAdjacentHTML('beforeend', '<%= j render partial: "shared/tables/table_wo_bundle/row", collection: @operations, as: :item %>');
<% if @operations.next_page %>
  document.querySelector('.pagination').innerHTML = '<%= j will_paginate @operations %>';
<% else %>
  document.querySelector('.pagination').remove();
<% end %>
